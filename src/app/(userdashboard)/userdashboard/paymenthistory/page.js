import PaymentHistory from '@/components/PaymentHistory/PaymentHistory';

const Payment = () => {
    return (
        <div>
            <PaymentHistory/>
        </div>
    );
};

export default Payment;